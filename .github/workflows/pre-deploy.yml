name: Pre-Deployment Validation

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

jobs:
  pre-deploy-checks:
    name: Run Pre-Deployment Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          cd functions
          npm ci

      - name: Validate Firebase Configuration
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Validating Firebase Configuration"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          echo "✓ Checking firebase.json..."
          test -f firebase.json && echo "  ✅ Found" || (echo "  ❌ Missing" && exit 1)

          echo "✓ Checking firestore.rules..."
          test -f firestore.rules && echo "  ✅ Found" || (echo "  ❌ Missing" && exit 1)

          echo "✓ Checking functions/index.js..."
          test -f functions/index.js && echo "  ✅ Found" || (echo "  ❌ Missing" && exit 1)

          echo ""

      - name: Run Syntax Checks
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔍 Running Syntax Checks"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          echo "✓ Checking JavaScript syntax..."
          node --check functions/index.js && echo "  ✅ Valid" || (echo "  ❌ Syntax errors" && exit 1)

          echo ""

      - name: Run Security Audit
        continue-on-error: true
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔒 Running Security Audit"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          cd functions
          npm audit --audit-level=high || echo "  ⚠️  Security warnings found (review recommended)"

          echo ""

      - name: Run Function Tests
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🧪 Running Function Tests"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          cd functions
          npm test

          echo ""

      - name: Generate Deployment Report
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 Deployment Readiness Report"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Environment: ${{ inputs.environment }}"
          echo "Date: $(date)"
          echo "Commit: ${{ github.sha }}"
          echo ""
          echo "Checks Performed:"
          echo "  ✅ Firebase Configuration"
          echo "  ✅ Syntax Validation"
          echo "  ✅ Security Audit"
          echo "  ✅ Function Tests"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎉 Ready for deployment!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "To deploy, run:"
          echo "  firebase deploy"
